# ผลตรวจ V3 — 16 กันยายน 2569

## รอบนี้: ถอดโซน AA-AF, AG, HB ที่เลิกใช้ (21/09/2569)

ผู้ใช้แจ้งว่าสามโซนนี้ไม่ได้ใช้แล้ว · 16 โซนเหลือ 13 โซน · Target 23 คีย์เหลือ 20 คีย์
กลุ่ม Mezzanine ถูกถอดทั้งกลุ่มเพราะมีโซนเดียวคือ HB

### วัดก่อนลงมือ — งานสุดท้ายของแต่ละโซน และค่าดิบที่ต้นทางเขียน

| โซน | ค่าดิบใน Position | แถว | แถวเข้าเฉลี่ย | Total Pick | คน | วันสุดท้ายที่มีงาน |
|---|---|---|---|---|---|---|
| AA-AF | `AA` | 673 | 646 | 593,582 | 12 | 19/07/2026 |
| AG | `AG-AH` | 1,673 | 1,284 | 1,428,081 | 15 | 19/07/2026 |
| HB | `HB-HA` | 392 | 350 | 307,853 | 3 | 18/07/2026 |

### จุดที่ต้องระวัง และเป็นเหตุให้ตัวเลขของโซนอื่นขยับ

**ค่าดิบของ AG คือ `AG-AH` ไม่ใช่ `AG` เปล่า ๆ** การจับโซนของ V1 หยิบโซนแรกที่รหัสตรง
เดิม `fullRackAg` มาก่อน `fullRackAhAi` จึงกินแถวไปทั้ง 1,673 แถว
เมื่อถอด AG ออก รหัส `AH` ที่เหลือในค่าดิบจึงพาแถวไปเข้า **AH-AI** ไม่ใช่กอง Not Found
จำลองก่อนแก้แล้วยืนยันตรงกับผลจริงหลังแก้ทุกตัว

| แถวของโซนที่ถอด | ไปอยู่ที่ | จำนวน |
|---|---|---|
| `AG-AH` | AH-AI | 1,673 |
| `AA` + `HB-HA` | Not Found | 1,065 |

### ตัวเลขก่อน/หลัง (ทั้งชุดข้อมูล) — จำลองไว้ก่อน แล้วตรงกับของจริง

| ค่า | ก่อนถอด | จำลองว่าจะได้ | หน้าเว็บจริงหลังถอด |
|---|---|---|---|
| จำนวนโซน | 16 | 13 | **13** |
| AH-AI · Productivity | 172.3 | 160.4 | **160.4** |
| AH-AI · แถวเข้าเฉลี่ย | 2,700 | 3,984 | **3,984** |
| AH-AI · พนักงาน | 41 | 53 | — |
| Not Found · แถวเข้าเฉลี่ย | 4,395 | 5,391 | **5,391** |
| Not Found · Productivity | 137.7 | 133.4 | **133.4** |
| **Total Pick ทั้งชุด** | 22,876,467 | 22,876,467 | **22,876,467** |

ยอดรวมไม่เปลี่ยนเลย ยืนยันว่า **ไม่มีผลงานหายไป** แค่ย้ายถัง ตามกฎ V1 ที่ไม่ลบผลงานย้อนหลัง

### ตรวจบนเบราว์เซอร์จริง

| สิ่งที่ตรวจ | ผล |
|---|---|
| จำนวนคีย์ Target ทั้ง 4 ไฟล์ | `script.js` 20 · `targets.json` 20 · `apps-script-target-write.gs` 20 · ฟอร์ม 20 — **ตรงกันหมด ไม่มีคีย์ที่ถอดเหลือ** |
| รายการโซนในโค้ด | 13 โซน · ไม่มี AA-AF / AG / HB |
| ตารางอันดับรายโซน (Efficiency) | 14 แถว (13 โซน + Not Found) · ไม่มีโซนที่ถอด |
| แผนผังโซน | 14 ช่อง · ไม่มีโซนที่ถอด |
| หน้าต่างตั้งค่า Target | 20 ช่อง · หัวข้อเป็น "(13 Zone)" · หัวข้อกลุ่มแก้เป็น "Micro Rack / Pick to Sort" (ไม่มี Mezzanine แล้ว) |
| `TARGETS.mezzanine` | **ยังอยู่** ตามที่ตั้งใจ เพราะแกนประเภทงานใช้ผ่าน `getPickTypeTarget()` |
| กวาดทุกหน้า | 13 หน้า active ได้ครบ · ไม่มีกราฟใบไหนแบน · **ไม่มี console error** |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ |

### ข้อเท็จจริงที่เจอเพิ่ม ยังไม่ทำอะไร

ประเภทงาน (Pick Type คอลัมน์ AK) ค่า **Mezzanine ไม่มีแถวเลยทั้งชุดข้อมูล**
จึงเป็นแกนที่ตายแล้วเหมือนกัน แต่ไม่ถอดในรอบนี้เพราะกระทบโครง payload ของ V1
(`CATEGORY_CONFIG` / `PICK_TYPE_DETAILS` / `finalizeBucket_`) — รอสั่งแยก

### สิ่งที่ไม่แตะ

**ไม่แก้ `v1-engine.js`** ตามข้อตกลง — engine ยังคิดถังของสามโซนนี้อยู่
แต่ `script.js` ไม่อ่านถังนั้นแล้ว ถังที่ไม่มีใครอ่านจึงถูกทิ้งไปเอง
ไม่แก้สูตรใด ๆ · ไม่แตะ `metrics.js`

## รอบก่อนหน้า: ถอดหน้า Incentive ออก (21/09/2569)

ถอดตามคอนเวนชันเดิมของโปรเจกต์ — **ย้าย `<section>` ไปเก็บที่ `parked/sections.html`
ไม่ลบตัวเรนเดอร์และไม่ลบสูตร** เอา section กับปุ่มเมนูกลับมาแล้วหน้าทำงานทันที

- ลบปุ่มเมนูในกลุ่ม 3 และย้าย `section#tab-incentive` ไป `parked/sections.html`
  พร้อมหัวข้อกำกับวิธีนำกลับ
- **`v2-incentive.js` ยังอยู่ครบ และ `index.html` ยังโหลดไฟล์นั้น** ตามคอนเวนชัน
  เพราะไฟล์ `return` ทั้งตัวทันทีเมื่อหา `#v3Incentive` ไม่เจอ (`v2-incentive.js:21-22`)
  ตรวจบนเบราว์เซอร์แล้ว **ไม่ประกาศ global ที่เกี่ยวกับ incentive เลย** และไม่ผูก listener
- คง key `incentive` ใน `TITLES` ของ `v2-shell.js` ไว้ (เหมือนที่คงของ 7 หน้าที่ถอดไปก่อนหน้า)
- แก้หน้าคู่มือที่ยังลิสต์ Incentive อยู่ — ตัดแถวในตาราง "เมนูไหนไว้ดูอะไร"
  และเปลี่ยนหัวข้อ "เว็บนี้ทำอะไรไม่ได้" ให้บอกว่าถอดหน้านี้ออกแล้ว
  พร้อมแก้บรรทัดเรื่องการเขียนกลับ Sheet ให้ตรงกับของจริง (ตอนนี้บันทึก Target ของทีมได้)

### ตรวจบนเบราว์เซอร์จริง

| สิ่งที่ตรวจ | ผล |
|---|---|
| จำนวนปุ่มเมนู / `.tab-panel` | **13 / 13 ตรงกัน** (เดิม 14 / 14) และชื่อ tab ตรงกันทุกตัว |
| ร่องรอย Incentive ในหน้า | ปุ่มเมนู ไม่มี · `#tab-incentive` ไม่มี · `#v3Incentive` ไม่มี |
| `v2-incentive.js` ที่ยังโหลดอยู่ | โหลดจริง แต่ `Object.keys(window)` ไม่มีคีย์ที่เกี่ยวกับ incentive เลย — inert ตามที่ออกแบบ |
| หน้าคู่มือ | ตาราง "เมนูไหนไว้ดูอะไร" ไม่มีแถว Incentive แล้ว · หัวข้อข้อจำกัดขึ้นว่า "ถอดหน้านี้ออกแล้ว (21/09/2569)" |
| กวาดทุกหน้า | 13 หน้า active ได้ครบ · ไม่มีกราฟใบไหนแบน · **ไม่มี console error** |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ |

### สิ่งที่ไม่แตะ

ไม่ลบ `v2-incentive.js` · ไม่ลบสูตรหรือเกณฑ์ Tier ใด ๆ · ไม่แก้ `v1-engine.js` / `metrics.js`
· ไม่แตะ Target ของทีม · ค่า `pickProductivityIncentiveConfig:v3` ที่เคยเก็บในเครื่องผู้ใช้ยังอยู่
(ไม่ลบให้ เพราะถ้านำหน้ากลับมาจะได้เกณฑ์เดิมคืน)

## รอบก่อนหน้า: รวม Zone AN, CA, BN, DA เป็นโซนเดียว (21/09/2569)

ข้อมูลหลังบ้านแก้ใหม่ — AN, CA, BN, DA เป็นโซนเดียวกัน (Half Rack ของ Max mart)
รวม `halfRackAnCa` + `halfRackBnDa` เป็น `halfRackAnCaBnDa` label `AN-CA-BN-DA` · 17 โซนเหลือ 16 โซน

### สิ่งที่วัดได้จากข้อมูลจริง และเป็นตัวกำหนดว่าการรวมปลอดภัย

ค่าที่พบจริงในคอลัมน์ Position ของแถวที่เข้าโซนกลุ่มนี้ **มีแค่ 2 แบบ**

| ค่าใน Position | ผลการจับโซนเดิม |
|---|---|
| `AN-CA-BN-DA` | มี `AN` → เข้าโซน `AN-CA` |
| `AN-BN` | มี `AN` → เข้าโซน `AN-CA` |

ทั้งสองแบบมี `AN` จึงถูกจับเข้า `AN-CA` มาตลอด และ **โซน `BN-DA` ไม่เคยมีแถวเลย**
(เป็นโซนผี ไม่โผล่ในตารางเพราะโค้ดกรอง `s.rows > 0` อยู่แล้ว)
การรวมครั้งนี้จึง **ไม่เปลี่ยนตัวเลขใด ๆ** แค่แก้ป้ายให้ตรงของจริงและตัดโซนผีออก

### ตัวเลขก่อน/หลังรวม (คิดซ้ำจากแถวดิบในเบราว์เซอร์ ทั้งชุดข้อมูล)

| ค่า | ชุดรหัส `AN,CA` | ชุดรหัส `BN,DA` | ชุดรวม `AN,CA,BN,DA` | หน้าเว็บแสดงหลังรวม |
|---|---|---|---|---|
| แถวต้นทาง | 790 | 790 | 790 | — |
| แถวเข้าเฉลี่ย | 736 | 736 | 736 | **736** |
| Total Pick | 1,399,998 | 1,399,998 | 1,399,998 | **1,399,998** |
| Productivity | 149.7 | 149.7 | 149.7 | **149.7** |
| พนักงาน | 6 | 6 | 6 | **6** |
| % Efficiency (Target 140) | 106.9% | — | 106.9% | **106.9%** |

สามชุดให้เลขเท่ากันเพราะเป็น **แถวชุดเดียวกันทั้งหมด** (ไม่ใช่เซตที่แยกกันแล้วเอามาบวก)
ดังนั้นการทดสอบว่า "AN-CA + BN-DA = ชุดรวม" แบบบวกกันจึงใช้ไม่ได้ และ **ไม่ได้แปลว่าผิด**

### ไม่นับซ้ำ — ตรวจที่ตัวจับโซนของ V1 แล้ว

`findZoneMatch_()` ใน `v1-engine.js` วนหาโซนแรกที่รหัสตรงแล้ว `return` ทันที
**แถวหนึ่งลงถังเดียวเสมอ** จึงบวก `halfRackAnCa` + `halfRackBnDa` ได้โดยไม่นับซ้ำ
(ถังหลังเป็น 0 อยู่แล้วกับข้อมูลชุดนี้ แต่ถ้าวันหน้ามีแถว `BN`/`DA` ล้วน ๆ จะถูกดึงเข้าโซนรวมให้เอง ไม่หลุดหาย)
และถ้าวันหน้า engine ส่งคีย์รวมมาเอง `script.js` จะใช้ของ engine ก่อน ไม่บวกต้นทางซ้ำ

### ตรวจบนเบราว์เซอร์จริง

| สิ่งที่ตรวจ | ผล |
|---|---|
| จำนวนคีย์ Target ทั้ง 4 ไฟล์ | `script.js` 23 · `targets.json` 23 · `apps-script-target-write.gs` 23 · ช่องในฟอร์ม 23 — **ตรงกันหมด ไม่มีคีย์เก่าค้าง** |
| ตารางอันดับรายโซน (หน้า Efficiency) | `AN-CA-BN-DA` แถวเดียว Target 140 · ไม่มี `AN-CA` หรือ `BN-DA` เดิมเหลือ |
| แผนผังโซน | 17 ช่อง (16 โซน + Not Found) · มี `AN-CA-BN-DA` · ไม่มีโซนเก่า |
| หน้าไม่ถึงเป้า | โชว์ `AN-CA-BN-DA` · ไม่มีป้ายเก่าเหลือ |
| หน้าเจาะลึกรายบุคคล | เรนเดอร์ครบ และอ้างโซนใหม่ |
| หน้าต่างตั้งค่า Target | 23 ช่อง · หัวข้อเป็น "Target แต่ละ Zone ย่อย (16 Zone)" · ช่องเขียนว่า `Zone AN-CA-BN-DA (Max mart)` = 140 |
| ย้ายค่าที่เคยตั้งไว้ (จำลอง AnCa=188, BnDa=133) | `halfRackAnCaBnDa` = **188** (AnCa ชนะตามลำดับใน `RENAMED_TARGET_KEYS`) · คีย์เก่าไม่ติดมาในผลลัพธ์ |
| กวาดทุกหน้า | 14 หน้า active ได้ครบ · ไม่มีกราฟใบไหนแบน · **ไม่มี console error** |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ |

### สิ่งที่ไม่แตะ

**ไม่แก้ `v1-engine.js`** ตามข้อตกลง (ยังมี `halfRackAnCa` กับ `halfRackBnDa` แยกกันอยู่)
ไม่แก้สูตรใด ๆ · ไม่แตะ `metrics.js` · ชื่อ Max mart ใส่ไว้ใน `title` และป้ายในฟอร์มเท่านั้น
**ไม่ใส่ใน `label`** เพราะ `insights.js` แยกรหัสโซนจาก `label.split('-')` ถ้าใส่ชื่อลงไปจะจับโซนพัง

## รอบก่อนหน้า: กดบันทึก Target ของทีมจากหน้าเว็บได้เลย (21/09/2569)

ผู้ใช้เลือกวิธี "กดบันทึกบนหน้าเว็บ" จึงเพิ่มปลายทางเขียนตัวใหม่
`apps-script-target-write.gs` เขียนลงแท็บ `V3 Target` ในสเปรดชีตเดียวกับ Results Master
แล้วหน้าเว็บอ่านแท็บนั้นกลับมาแบบอ่านอย่างเดียว — **การอ่านยังไม่ผ่าน Apps Script เลย**
ถ้าสคริปต์พังหรือถูกลบ หน้าเว็บยังอ่าน Target ได้ตามปกติ แค่บันทึกใหม่ไม่ได้

### ตรวจความสอดคล้องของคีย์ (ถ้าไม่ตรงกัน จะเขียนค่าผิดช่องแบบเงียบ ๆ)

| ไฟล์ | จำนวนคีย์ | ตรงกันไหม |
|---|---|---|
| `apps-script-target-write.gs` (`TARGET_KEYS`) | 24 | — |
| `script.js` (`DEFAULT_TARGETS`) | 24 | ✅ ตรงกับ .gs |
| `data/targets.json` (`targets`) | 24 | ✅ ตรงกับ .gs |

ไม่มีคีย์ขาดและไม่มีคีย์เกินทั้งสามไฟล์ · ช่วงค่าที่สคริปต์ยอมรับ 1–1000

### ตรวจบนเบราว์เซอร์จริง (จำลองปลายทางด้วยการ stub `window.fetch`)

ยังไม่ได้ Deploy ของจริง จึงยังไม่ได้ทดสอบกับ Apps Script ตัวเป็น ๆ
ที่ทดสอบคือ **โค้ดฝั่งเว็บทุกเส้นทาง** ด้วยการจำลองคำตอบของปลายทาง

| กรณี | ผล |
|---|---|
| ยังไม่ตั้ง `write.url` (สภาพที่ push ไป) | ปุ่มบันทึก **ไม่โผล่เลย** (0 ปุ่ม) · ไม่ยิงไปอ่านแท็บ (`meta.from = file`) · ไม่มี error ใน console |
| อ่านแท็บ `V3 Target` (จำลอง CSV) | ได้ครบ **24/24 คีย์** · อ่าน `updatedAt` และ `updatedBy` ได้ · ค่าจากแท็บชนะค่าในไฟล์ |
| ตั้ง `write` แล้วแต่ค่าตรงกับทีม | ปุ่มโผล่แต่ **disabled** พร้อมข้อความ "ค่าที่เห็นตรงกับของทีมอยู่แล้ว" |
| ปรับเป้าเอง (พิมพ์ 165) | ชิปเป็นส้มและมี **2 ปุ่ม**: บันทึกให้ทั้งทีม · ใช้ค่าของทีม · กล่องในหน้าต่างตั้งค่าขึ้น "(1 ช่อง)" |
| กดบันทึก (สำเร็จ) | POST ส่ง token ถูก + **24 คีย์ครบ** + ค่า 165 · ชิปกลับเขียวเป็น `21/09/2569 14:05` · `targetOverride` = false · ขึ้น "บันทึกเป็นค่าของทีมแล้ว 2 ช่อง" |
| กดบันทึก (โทเคนผิด) | ขึ้น "บันทึกไม่สำเร็จ: โทเคนไม่ถูกต้อง · ค่าที่ปรับยังมีผลกับเครื่องนี้" · ค่ายังเป็น 171 ในเครื่อง · ยังเป็นสถานะ override ไม่หลอกว่าบันทึกแล้ว |
| กวาดทุกหน้าหลังแก้ | 14 หน้า active ได้ครบ · ไม่มีกราฟใบไหนแบน · ไม่มี console error |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ |

### จุดที่ตัดสินใจไว้ และเหตุผล

- **อ่านแท็บด้วย gviz ระบุชื่อแท็บ** ไม่ใช่ CSV export ที่ต้องรู้ `gid`
  เพราะแท็บนี้สคริปต์สร้างเองตอนบันทึกครั้งแรก ผู้ใช้จึงไม่ต้องไปหา `gid` มาให้
  เหตุที่ `source.js` เลี่ยง gviz (ตัดแถวตาม filter / รหัสผสมตัวอักษรหาย) ไม่กระทบที่นี่
  เพราะแท็บมีแค่ชื่อคีย์กับจำนวนเต็ม 24 แถว ไม่มี filter และไม่มีรหัสพนักงาน
- **ยอมรับค่าจากแท็บเฉพาะตอนได้ครบ 24 คีย์** ถ้าได้ไม่ครบให้ถอยไปใช้ไฟล์
  เพราะ Target ครึ่ง ๆ กลาง ๆ อ่านผิดยิ่งกว่าไม่ใช้เลย
- **ยิงไปอ่านแท็บก็ต่อเมื่อตั้ง `write.url` แล้ว** ถ้ายังไม่ตั้ง แท็บยังไม่มี
  การยิงไปจะได้ error เปล่า ๆ ทุกครั้งที่เปิดหน้า
- **POST ส่งเป็น `text/plain`** ให้เป็น simple request ไม่มี preflight
  เพราะ Apps Script ตอบ 302 ไป `googleusercontent` ซึ่ง preflight ผ่านไม่ได้ — อย่าเปลี่ยนเป็น `application/json`
- **ปุ่มบันทึกอยู่บนชิปด้วย** ไม่ใช่แค่ในหน้าต่างตั้งค่า เพราะฟอร์มตั้งค่าปิดตัวเองหลังกดบันทึก
  ผู้ใช้จะต้องเปิดใหม่อีกรอบถ้าปุ่มอยู่แต่ในนั้น

### ความเสี่ยงที่รู้ตัว ยังไม่แก้

`write.url` กับ `write.token` ถูก commit ลง repo เพื่อให้ทุกคนกดบันทึกได้โดยไม่ต้องตั้งค่าเอง
ใครเปิดเว็บหรือเปิด repo ได้จึงเปลี่ยน Target ได้ด้วย
ตัวกันความเสียหาย: สคริปต์เขียนได้แค่แท็บ `V3 Target` กับแท็บ log · รับแค่ 24 คีย์ ค่า 1-1000
· คีย์แปลกปลอมหรือค่านอกช่วงปฏิเสธทั้งคำขอ ไม่เขียนบางส่วน · log ทุกช่องที่เปลี่ยนพร้อมเวลาและผู้บันทึก
· และ version history ของ Google Sheet ย้อนได้
ถ้าไม่ยอมรับความเสี่ยงนี้ ให้ย้าย `write` ไปเก็บใน `localStorage` ของแต่ละเครื่อง (แลกกับที่คนอื่นกดบันทึกไม่ได้)

### ยังไม่ได้ทดสอบ

ยังไม่ได้ Deploy Apps Script ตัวจริง จึงยังไม่ได้ยืนยัน 3 อย่างนี้กับของจริง —
การสร้างแท็บครั้งแรก · CORS ตอน POST ข้ามโดเมนจริง · และรูปแบบ `updatedAt` ที่ Sheet เขียนกลับมา
ต้องตรวจซ้ำหลัง Deploy

## รอบก่อนหน้า: ตัดขั้นตอนในหน้าต่างตั้งค่า Target ให้เหลือแค่วันที่อัปเดต (21/09/2569)

ผู้ใช้บอกว่ากล่องในหน้าต่างตั้งค่า "ยากกับคนใช้" ขอให้เหลือแค่วันอัปเดตล่าสุด
ถอดทุกอย่างที่เรียกให้ผู้ใช้ไปทำอะไรต่อออก

- ถอดปุ่ม **📤 คัดลอกค่าที่เห็นอยู่เป็นไฟล์ targets.json** · ปุ่ม **คืนค่าเดิมของเครื่องนี้**
  · บรรทัด "ตรงกับค่าของทีม" · ย่อหน้าอธิบายให้ส่งค่าไปให้ผู้ดูแล · โน้ตในไฟล์
- กล่อง `#targetTeamInfo` เหลือบรรทัดเดียว `อัปเดตล่าสุด <b>21/09/2569</b>`
  (แปลง `updatedAt` เป็น d/m/พ.ศ. ให้ตรงรูปแบบวันที่ที่ใช้ทั้งเว็บ)
- ชิปบนแถบตัวกรองยุบจาก 4 สถานะเหลือ 3 — ถอดสถานะฟ้า (`reset`) ที่บอกว่าค่าเดิมต่างกี่ช่อง
  และถอดปุ่ม "ดู / คัดลอก" ออก เหลือปุ่ม "ใช้ค่าของทีม" ตอนปรับเองเท่านั้น
- ลบโค้ดที่ไม่ได้ใช้แล้วทิ้ง (`teamTargetsFileText` · `copyTeamTargetsText` · `readLocalBackupTargets`
  · `localBackupTargets`) และลบคีย์ `pickProductivityTargetsLocalBackup:v3` ให้เองตอนโหลดหน้า
  ไม่ค้างเป็นขยะในเครื่องผู้ใช้
- ตัดสไตล์ที่ไม่ได้ใช้แล้วออกจาก `v2-theme.css` (`[data-state="reset"]` · `.tt-ok`
  · `.target-team-info button` · `.target-team-info textarea`) — ไฟล์เล็กลง 449 ไบต์
- แก้หน้าคู่มือที่ยังสอนวิธีเก่าอยู่ (ตารางชิปเหลือ 3 แถว · หัวข้อ "อยากเปลี่ยนเป้าให้ทั้งทีม"
  เลิกบอกขั้นตอนกดคัดลอก)

### ตรวจบนเบราว์เซอร์จริง

| สิ่งที่ตรวจ | ผล |
|---|---|
| กล่องในหน้าต่างตั้งค่า | `<p class="tt-note">อัปเดตล่าสุด <strong>21/09/2569</strong></p>` · ปุ่ม/textarea ในกล่อง = **0** |
| ชิปตอนตรงกับทีม | `✅ Target ของทีม · 21/09/2569` · ปุ่มในชิป = **0** |
| ปรับเป้าเอง (พิมพ์ 188) | ชิปเป็นส้ม `🔶 Target ส่วนตัว · คนอื่นไม่เห็นเลขชุดนี้` + ปุ่ม ใช้ค่าของทีม |
| กดปุ่ม ใช้ค่าของทีม | กลับเป็น 150 · ชิปกลับเขียวพร้อมวันที่ · ช่องบนแถบกรองตามไปด้วย |
| คีย์ backup เก่าในเครื่อง | ถูกลบแล้ว (`localStorage.getItem(...)` = `null`) |
| หน้าคู่มือ | ไม่พูดถึง "คัดลอก" และไม่มีสถานะ "ฟ้า" อีก · ยังโชว์วันที่ 21/09/2569 |
| console | ไม่มี error |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ |

### ที่ยังค้าง

ผู้ใช้ขอให้ **แก้ค่าของทีมจากหน้าเว็บได้เลย** ซึ่งทำไม่ได้ด้วยโครงปัจจุบัน
(static + อ่าน Sheet แบบ read-only ไม่มีเซิร์ฟเวอร์ของตัวเอง) ต้องเพิ่มปลายทางเขียนก่อน
และการทำแบบนั้นกลับข้อตกลง 17/09/2569 ที่ว่า V3 ไม่เรียก Apps Script — ยังรอเลือกวิธี

## รอบก่อนหน้า: ใส่ค่า Target จริงของทีมลง data/targets.json (21/09/2569)

ผู้ใช้ส่งค่าที่ตั้งไว้เองมาจากปุ่มคัดลอกในหน้าเว็บ ใส่ลงไฟล์กลางตามที่ส่งมาทุกค่า

- 24 คีย์ครบ ตรงกับช่องในหน้าต่างตั้งค่าทั้ง 24 ช่อง (ไม่ขาด ไม่เกิน) และผ่าน sanitize ทุกค่า
- โหลดบนเบราว์เซอร์แล้วค่าเข้าครบ **23 ช่องที่ต่างจากค่าเดิม** · ช่องเป้าบนแถบตัวกรองขึ้น 150
  · ชิปขึ้น `✅ ใช้ Target ของทีมแล้ว · ค่าเดิมของเครื่องนี้ต่าง 23 ช่อง` · ไม่มี console error
- ผลที่ขยับตามค่าใหม่ (ทั้งชุดข้อมูล): Efficiency เฉลี่ยรวม **94.2%** (Productivity 141.3 เทียบเป้า 150)
  · วันที่ผ่านเกณฑ์ **45 / 243 วัน (18.5%)** · ป้ายเมนูไม่ถึงเป้า **169 คน**
  · อันดับรายโซน ถึงเป้า 9 โซน / ไม่ถึงเป้า 8 โซน

### ค่าที่ตั้งไว้ 1 หยิบ/ชม. 3 ช่อง — รายงานไว้ ยังไม่แก้เพราะเป็นค่าที่ผู้ใช้ส่งมา

`mezzanine` · `fullRackAaAf` · `mezzanineHb` ตั้งไว้ที่ `1`
ผลที่วัดได้จากตารางอันดับรายโซน (ทั้งชุดข้อมูล)

| โซน | Productivity | Target | % Efficiency | ผล |
|---|---|---|---|---|
| AA-AF (Full Rack) | 119.1 | 1 | **11,906.3%** | ขึ้นเป็นอันดับ 1 ของตารางโซน |
| HB (Mezzanine) | 105.7 | 1 | **10,569.4%** | ขึ้นเป็นอันดับ 2 |

สองโซนนี้ดันโซนที่ทำได้ดีจริง (BG-BH 173.6% · AG 135.2%) ตกไปอันดับ 3-4
ถ้าเจตนาคือ "ไม่ต้องตามโซนนี้" ก็ทำงานตามที่ตั้ง แต่ถ้าพิมพ์พลาด ให้แก้ไฟล์แล้ว push ใหม่

## รอบก่อนหน้า: ติดดาวเมนูแนะนำ + หน้าคู่มือการใช้เว็บ (21/09/2569)

ติดดาว ⭐ ที่เมนู **ภาพรวมวันนี้** และ **แผนผังโซน** และเพิ่มกลุ่ม 4 ช่วยเหลือ
พร้อมหน้า **คู่มือการใช้เว็บ** (`v2-guide.js` วาดลง `#v3Guide`) เป็นเมนูล่างสุด

### ตรวจบนเบราว์เซอร์จริง

| สิ่งที่ตรวจ | ผล |
|---|---|
| จำนวนปุ่มเมนูกับจำนวน `.tab-panel` ต้องเท่ากัน | 14 / 14 ตรงกัน (เดิม 13 / 13) |
| ดาวขึ้นที่เมนูไหน | `overview` และ `zone-map` เท่านั้น · ชิดขวาของปุ่ม ไม่ดันชื่อเมนูตก |
| ชื่อหน้าบน `.pagehead` | `📖 คู่มือการใช้เว็บ` (เพิ่ม key `guide` ใน `TITLES`) |
| เนื้อหาที่วาดได้ | 10 การ์ด · 7 ตาราง (30 แถว) · 14 หัวข้อพับ `<details>` · 16 KB |
| ค่าจริงที่คู่มือดึงมาเติม | ช่วงข้อมูล 02/01/2026–21/09/2026 · 244 วัน · 19,936 แถว · 22,876,467 ชิ้น · เป้ารวม 170 / Half Rack 200 / Training 100 · สถานะ "ตรงกับค่าของทีม" |
| กวาดทุกหน้าหลังเพิ่มเมนู | ทั้ง 14 หน้า active ได้ · ชื่อหน้าถูกทุกหน้า · กราฟรวม 28 ใบ **ไม่มีใบไหนสูง 0 (แบน)** · ไม่มี console error |
| `node tests/calculation.test.cjs` | PASS ทุกข้อ · 15/09/2026 = 82,958 / 144.9 / 66 ไม่ขยับ |

### บั๊กที่เจอจากการตรวจ และแก้แล้ว

- **หน้าคู่มือดันให้เลื่อนแนวนอน** วัดได้ว่ามี 224 element ล้นขอบขวา (`scrollWidth` 1360 vs `clientWidth` 1265)
  ขณะที่หน้าภาพรวมล้น 0 element จึงเป็นของใหม่ล้วน
  สาเหตุ: `.affiliation-table` ของ V2 ตั้ง `min-width: 1040px` ไว้สำหรับตารางข้อมูลหลายคอลัมน์
  แล้ว grid item มี `min-width: auto` ตามค่าตั้งต้น จึงดันการ์ดให้กว้างตามตาราง
  แก้ที่ชั้น `v2-theme.css` (ไม่แก้ `v2-base.css` ตามข้อตกลง): `.guide-wrap > * { min-width: 0 }`
  และ `.guide-table { min-width: 0 !important; table-layout: fixed }`
  ตรวจซ้ำ: desktop ล้น 0 element · มือถือ 375×812 `scrollWidth` = `clientWidth` = 375 ไม่ล้นแนวนอน

### สิ่งที่ไม่แตะ

ไม่แก้สูตรใด ๆ · ไม่แตะ `v1-engine.js` / `metrics.js` / `v2-base.css` · หน้าคู่มือไม่คิดเลขเองเลย
อ่านค่าที่คิดไว้แล้วมาเติมในข้อความเท่านั้น · ไม่ใช้ชื่อคอลัมน์ของ Sheet ในข้อความตามที่ถอดออกไปแล้วทั้งเว็บ

## รอบก่อนหน้า: Target ของทีม ให้ทุกคนเห็นเลขชุดเดียวกัน (21/09/2569)

ปัญหาที่แก้: Target เก็บอยู่ใน `localStorage` / cookie / IndexedDB ของเครื่องแต่ละคน
คนที่ปรับ Target เห็นเลขชุดหนึ่ง คนที่เพิ่งเปิดเว็บเห็นค่าเดิม แล้วมาถามกันว่าทำไมไม่เหมือนกัน
ย้ายแหล่งความจริงไปที่ไฟล์ `data/targets.json` ที่ push ขึ้นไปพร้อมเว็บ และให้ทับค่าในเครื่องเสมอ

### ตรวจบนเบราว์เซอร์จริง (http://127.0.0.1:8093)

| กรณี | ผล |
|---|---|
| เครื่องสะอาด เปิดหน้าแรก | ชิปเขียว `✅ Target ของทีม · 2026-09-21` · `TARGETS.overall` = 170 · ช่องบนแถบกรอง = 170 |
| เครื่องที่มีค่าเก่าค้าง (จำลอง overall 155 · halfRack 185 · halfRackCdCe 190) | ค่าของทีมทับสำเร็จทั้ง 3 ช่อง → 170 / 200 / 200 · `localStorage` ถูกเขียนเป็นค่าทีม · backup เก็บค่าเดิมครบ 24 คีย์ · ชิปฟ้า `ใช้ Target ของทีมแล้ว · ค่าเดิมของเครื่องนี้ต่าง 3 ช่อง` |
| ปรับ Target เองหลังรับค่าทีม (พิมพ์ 190) | ชิปเปลี่ยนเป็นส้ม `🔶 Target ส่วนตัว · คนอื่นไม่เห็นเลขชุดนี้` ทันที |
| กดปุ่ม **ใช้ค่าของทีม** ในชิป | กลับเป็น 170 · ชิปกลับเป็นสถานะของทีม · ช่องบนแถบกรองตามไปด้วย |
| กดปุ่ม **คืนค่าเดิมของเครื่องนี้** ในหน้าต่างตั้งค่า | ได้ 155 / 190 คืนครบ · ชิปเป็นส้มตามจริง |
| ปุ่ม **📤 คัดลอกค่าที่เห็นอยู่เป็นไฟล์ targets.json** | ได้ JSON ครบทั้ง 24 คีย์ พร้อม `version` / `updatedAt` / `note` วางทับไฟล์ได้ทั้งก้อน · เครื่องที่ clipboard ใช้ไม่ได้ขึ้นกล่องข้อความให้คัดลอกมือ (ทดสอบแล้วตกลง fallback จริง) |
| ซ่อนไฟล์ `data/targets.json` (จำลอง 404) | **หน้าไม่ล่ม** `.kpis` ยังเรนเดอร์ · ถอยไปใช้ค่าในเครื่อง · ชิปแดง `⚠️ โหลด Target ของทีมไม่ได้ · ใช้ค่าในเครื่องนี้` · console มีแค่ 404 ที่คาดไว้ |

### เลขอ้างอิงไม่ขยับ

- `node tests/calculation.test.cjs` — PASS ทุกข้อ
  (02/01/2026 = 48,453 / 102.7 / 59 · 15/05/2026 = 85,775 / 120.3 / 82 · 15/09/2026 = 82,958 / 144.9 / 66 · 16/09/2026 = 37,455 / 167.7 / 27)
- อ่านจากหน้าเว็บจริงที่ 15/09/2026: Total Pick **82,958** · Productivity **144.9** · **66 แถวเข้าเฉลี่ย · 9 แถวไม่เข้า** · เป้าหมาย KPI **170** · Gap `ต่ำกว่า Target 25 Pick/Hr`
- ไม่มี console error ในทุกกรณีที่ไม่ใช่การจำลอง 404

### บั๊กที่เจอจากการตรวจ และแก้แล้ว

- ช่อง **🎯 เป้า Target** บนแถบตัวกรองค้างเลขเก่าของเครื่องนั้นอยู่พักหนึ่ง
  (วัดได้จริง: `TARGETS.overall` เป็น 170 แล้ว แต่ช่องยังโชว์ 155 จนกว่าข้อมูลชุดแรกจะมาถึง)
  เพราะ `v2-shell.js` ตามค่าใหม่เฉพาะตอน event `v3-render` ซึ่งมาหลังค่าของทีมเสมอ
  แก้โดยให้ `updateTargets()` ยิง event `v3-targets` ทันทีที่ค่าเปลี่ยน และ `v2-shell.js` ฟังเพิ่มอีกตัว
  ตรวจซ้ำแล้วช่องขึ้น 170 ตั้งแต่วินาทีแรก ไม่ค้าง 155 อีก

### สิ่งที่ไม่แตะ

ไม่แก้สูตรใด ๆ · ไม่แก้ `v1-engine.js` · ไม่แตะ `metrics.js` · ไม่เพิ่มการเขียนกลับ Google Sheet
และไม่เรียก Apps Script (ต่างจาก V2 ที่เก็บ shared targets ไว้ใน `PropertiesService`)
ค่า Target ตั้งต้นใน `data/targets.json` เท่ากับ `DEFAULT_TARGETS` เดิมทุกคีย์ ตัวเลขทั้งเว็บจึงไม่ขยับ

## รอบก่อนหน้า: สามหน้าใหม่จาก V2 + แก้กราฟแบนทั้งเว็บ (17/09/2569)

เพิ่ม Efficiency · เจาะลึกรายบุคคล · Incentive (เลือกจากการสำรวจ 17 หน้าของ V2 ว่าหน้าไหนทำได้ด้วยข้อมูล V1)
เขียนแยกไฟล์ละหน้า แล้วให้ตรวจแบบพยายามหาจุดพังทีละหน้า พบ 18 ประเด็น แก้ครบ

### ตรวจตัวเลขด้วยการคิดซ้ำจากแถวดิบในเบราว์เซอร์ (ช่วง 01/07–17/09/2026)

| ค่าที่ตรวจ | หน้าเว็บแสดง | คิดซ้ำเอง |
|---|---|---|
| Total Pick (คอลัมน์ E) | 6,833,170 | 6,833,170 |
| Productivity (สูตร V1) | 153.6 | 153.6371 |
| Efficiency เฉลี่ยรวม | 90.4% | 90.4% |
| วันที่ผ่านเกณฑ์ | 15 / 75 วัน | — |
| พนักงานที่ผ่านเกณฑ์ | 34 / 119 คน (28.6%) | 34 / 119 (28.6%) |
| คนที่ไม่ตัดสิน (count = 0) | 10 คน | 10 คน |
| คนทั้งหมดที่มีแถว | 129 คน | 129 คน |
| คนที่ % สูงสุด | 172730 · 628.0 หยิบ/ชม. จาก 1 แถวเข้าเฉลี่ย | 628.0 · 369.4% ของเป้า |

Incentive ช่วงเดียวกัน: ฿164,080 · ผ่านเกณฑ์ 33 / 119 คน (น้อยกว่าคนที่ถึงเป้า 34 คนอยู่ 1 คน
เพราะคนนั้นติดเกณฑ์ชั่วโมง/วันขั้นต่ำ ซึ่งเป็นพฤติกรรมที่ตั้งใจให้กันตัวเลขหลอก)

### Scorecard รายบุคคล ตรวจบนเบราว์เซอร์

กดชื่อในตารางเทียบทุกคน → เปิด Scorecard ได้จริง มีปุ่มกลับ · การ์ด 5 ใบ · กราฟเทรนรายวันและกราฟรายชั่วโมงวาดครบ (330px / 250px)
· ตารางโซนที่ทำ 9 คอลัมน์ · ตารางรายวัน 9 คอลัมน์รวมคอลัมน์ "เหตุผลที่ไม่เข้าเฉลี่ย"

### บั๊กที่พบจากการกวาดทุกหน้า และแก้แล้ว

- **กราฟได้ canvas สูง 0** เมื่อถูกวาดตอนแท็บยังซ่อน (`.tab-panel` เป็น display:none) และ Chart.js ไม่วัดใหม่ให้เอง
  เจอที่หน้าภาพรวม (7 กราฟ) หน้าช่วงเวลา และหน้าไม่ถึงเป้า ตอนสลับหน้าหลังเปลี่ยนตัวกรอง
  สาเหตุคือ `insights.js` ผูก listener ก่อน `v2-shell.js` ตามลำดับ script จึงเรนเดอร์ก่อนแท็บจะ active
  แก้สามชั้น: หน่วงเรนเดอร์ 60ms ใน insights.js · วาดกราฟภาพรวมใหม่เมื่อกลับเข้าหน้า · และ `resizeChartsIn()` ใน v2-shell.js
  **ผลหลังแก้: กวาดทั้ง 13 หน้า กราฟ 21 ใบ วาดครบ ไม่มีใบไหนแบน**
- **Chart instance ค้างสะสม** เพราะ `Chart.getChart('id')` หาไม่เจอหลัง `innerHTML` สร้าง canvas ใบใหม่
  (พิสูจน์ด้วย harness: เรนเดอร์ 5 ครั้ง ก่อนแก้ค้าง 15 ใบ หลังแก้เหลือ 3 ใบ) แก้ทั้งในหน้าใหม่และใน `belowTargetPage` ของ insights.js
- **Incentive เปิดมาเห็น ฿0 ทุกช่อง** เพราะค่าตั้งต้น "ต้องมีอย่างน้อย 3 วัน" ชนกับตัวกรองเริ่มต้นที่เป็นวันล่าสุดวันเดียว
  แก้แล้วสภาพเริ่มต้นใช้งานได้ (วันเดียวได้ ฿1,450 · 12/30 คน) และถ้าช่วงสั้นกว่าเกณฑ์จะบอกเหตุผลบนหน้า
- การ์ด/โน้ตที่เขียนไม่ตรงกับที่โค้ดคิด 5 จุด · ค่าที่ส่งลง CSV ไม่ตรงกับที่แสดง 2 จุด · สีที่ขอบเกณฑ์ขัดกันเพราะปัดทศนิยมก่อนตัดสิน
  · ค่าที่พิมพ์ในแผงตั้งค่าหายตอนเว็บรีเฟรชอัตโนมัติ · เกณฑ์ตัดสินการ์ดกับตารางไม่สอดคล้องกัน

`node tests/calculation.test.cjs` ผ่านทุกรอบ · 15/09/2026 = 82,958 / 144.9 / 66 แถว ไม่ขยับ · ไม่มี JavaScript error หรือ console.error

## รอบก่อนหน้า: การ์ดแถวที่อาจบันทึกซ้ำ (18/09/2569)

การ์ดใหม่อยู่บนสุดของหน้า Not Found & คุณภาพข้อมูล วาดโดย `v2-duplicate.js` ลงใน `#v3Duplicates`
ซึ่งเป็นกล่องแยกจาก `#v3Quality` เพราะหน้าคุณภาพเขียนทับ innerHTML ของตัวเอง

การ์ดนี้ไม่ตัดยอดและไม่แก้สูตร ทุกหน้ายังนับทุกแถวหนึ่งครั้งตามเดิม สแกนทุกแถวที่มีวันที่ทั้งชุด ไม่ขึ้นกับตัวกรอง

### สิ่งที่วัดได้ และเป็นตัวกำหนดหน้าตาการ์ด

- ธงซ้ำที่ต้นทางติดไว้ = "แถวนี้มีรหัสพนักงานกับวันที่ตรงกับแถวอื่น" เป๊ะ ๆ **ไม่ต่างกันแม้แถวเดียว**
  (ธง 1,530 แถว · แถวที่เว็บจับคู่ได้ 1,530 แถว · ไม่ตรงกัน 0 แถว) และเลขบนธงคือจำนวนแถวในกลุ่ม
  ธง 2 มี 1,428 แถว = 714 กลุ่มละ 2 แถว · ธง 3 มี 102 แถว = 34 กลุ่มละ 3 แถว รวม 748 กลุ่ม
- กะ โซน ประเภทงาน สังกัด BU **ใช้ตัดสินไม่ได้เลย** เพราะทุกช่องดึงมาจากทะเบียนด้วยรหัสพนักงาน
  แถวของคนเดียวกันจึงได้ค่าเหมือนกันโดยปริยาย — วัดแล้วไม่ต่างกันสักกลุ่มใน 748 กลุ่ม
  (แบบร่างแรกใช้ช่องพวกนี้เป็นตัวตัดสิน ทิ้งไปหลังวัด)
- ตัวตัดสินที่ใช้ได้จริงคือ **ชั่วโมงที่ทับกัน** เพราะยอดรายชั่วโมงเป็นค่าของแถวนั้นเอง

### ตัวเลขที่การ์ดแสดง ตรงกับการคิดซ้ำจากแถวดิบในเบราว์เซอร์ทุกค่า

| ค่า | การ์ด | คิดซ้ำเอง |
| --- | --- | --- |
| คู่ที่ซ้ำคีย์ (รหัส + วันที่) | 748 กลุ่ม | 748 |
| แถวเกินมา | 782 แถว | 782 |
| ชั่วโมงที่ทับกัน | 3,201 ชั่วโมง-คน | 3,201 |
| ยอดที่อาจเกินจริง | 370,786 ชิ้น (1.63% ของ 22,693,832) | 370,786 |
| เหมือนกันทุกอย่าง (ยอดและชั่วโมงตรงเป๊ะ) | 14 กลุ่ม | 14 |
| ไม่ทับชั่วโมงกันเลย | 174 กลุ่ม | 174 |
| ชั่วโมงรวมเกิน 24 ชม./วัน | 26 กลุ่ม | 26 |
| แถวซ้ำที่เข้าเฉลี่ย | 1,376 แถว | 1,376 |

14 กลุ่มที่เหมือนกันทุกอย่างกระจายอยู่ 4 วัน (23/01 สามกลุ่ม · 08/05 แปดกลุ่ม · 04/09 หนึ่งกลุ่ม · 16/09 สองกลุ่ม)
รวมยอดที่อาจเกินจริง 6,749 ชิ้น ตัวอย่าง 10124874 วันที่ 23/01 สองแถวเท่ากันทุกช่อง (771 ชิ้น 6 ชม. เฉลี่ย 129 ลง 1:00–7:00 ทั้งคู่)

กลุ่มที่ทับชั่วโมงกันกระจายทุกเดือนที่มีข้อมูล (ม.ค. 66 · ก.พ. 84 · มี.ค. 169 · เม.ย. 34 · พ.ค. 153 · ก.ย. 68)
ไม่ได้กระจุกที่เดือนเดียว

### ตรวจบนเบราว์เซอร์

- กด 7 การ์ดขึ้นครบ ตารางแบ่ง 50 แถวต่อหน้า และ `#v3Quality` ด้านล่างยังเรนเดอร์ครบ (ไม่ถูกเขียนทับ)
- กดหัวคอลัมน์ "ความน่าสงสัย" เรียง สูง→ต่ำ แล้วกดซ้ำได้ ต่ำ→สูง
- ค้นหารหัส 10084420 ได้ 10 แถว (คนนี้มีวันที่ซ้ำคีย์ 10 วัน)
- ไม่พบ error ใน console
- `node tests/calculation.test.cjs` ยังผ่าน 15/09/2569 = 82,958 / 144.9 / 66

## รอบก่อนหน้า: หน้าเทรน สัปดาห์/เดือน + เมนูไม่ถึงเป้าให้เด่น (17/09/2569)

### หน้าเทรน สัปดาห์ / เดือน

อ่านต้นฉบับ V2 ก่อน (`app.js` renderTrendPage 6151-6241) แล้วลอกองค์ประกอบมาครบ
**แต่เปลี่ยนสูตรเป็นของ V1 โดยตั้งใจ** เพราะ V2 คิด Productivity ของงวดเป็นเฉลี่ยของค่าเฉลี่ยรายวัน (average-of-average 2-3 ชั้น)

ตรวจด้วยการคิดซ้ำจากแถวดิบในเบราว์เซอร์

| ค่าที่ตรวจ | หน้าเว็บแสดง | คิดซ้ำจากแถวดิบ |
|---|---|---|
| สัปดาห์ 14/9–20/9 ยอดหยิบ | 316,024 | 316,024 |
| สัปดาห์ 14/9–20/9 Productivity | 151.2 | 151.2 (252 แถวเข้าเฉลี่ย) |
| วันทำการ / คน / ชั่วโมง | 4 วัน · 83 คน · 2,125.0 | 4 · 83 · 2,125.0 |
| เดือน ก.ย. 2569 ยอดหยิบ | 1,227,267 | 1,227,267 |
| เดือน ก.ย. 2569 Productivity | 144.6 | 144.6 (981 แถวเข้าเฉลี่ย) |
| Δ MoM ของ ส.ค. | -21.3 (-12.8%) | ก.ค. 166.5 → ส.ค. 145.2 = -21.3 |

- รายสัปดาห์กาง 38 สัปดาห์ รายเดือน 9 เดือน ไม่หุบตามตัวกรองวันที่ · แท่งงวดที่อยู่ในช่วงที่เลือกเข้มกว่า
- **โหมดรายวัน** (เพิ่มตามที่ถามว่าควรใส่ไหม): ตาราง 240 วัน · กราฟกางทีละเดือน ก.ย. ได้ 16 แท่ง
  กดย้อนเดือนได้เป็น ส.ค. ได้ 30 แท่ง (1/8 ถึง 31/8) และมีปุ่มกลับเดือนของวันที่ที่เลือก
  · ตรวจค่าอ้างอิงของงานนี้ในโหมดวัน: 15/09/2026 = 82,958 / 144.9 / 66 แถว ตรงกับที่คิดซ้ำจากแถวดิบ
  · 16/09 = 132,229 / 153.3 · วันล่าสุด 17/09 = 36,456 / 152.8
  · เครื่องหมายเตือนทำงานจริง: 22/01/2569 ขึ้น 204.0 หยิบ/ชม. จาก **3 แถวเข้าเฉลี่ย** ได้ ⚠️
    ถ้าไม่เตือนจะถูกอ่านเป็นวันที่ทำได้ดีเป็นอันดับ 6 ของทั้งชุด
- ตัวสลับจำค่าใน localStorage (`pickProductivityTrendPeriod:v3`) ทดสอบกดสลับแล้วรีโหลด ค่าคงเดิม
- กราฟทั้งสองใบสูง 330px เมื่อเปิดหน้า (เคยเสี่ยงได้ 0 เพราะ `.tab-panel` เป็น display:none จึงเรนเดอร์ตอนกดเมนู)
- ตารางใหม่เรียงตามหัวคอลัมน์ได้ ทดสอบเรียง Productivity มาก→น้อย ได้ ก.ค. 166.5 → ส.ค. 145.2 → ก.ย. 144.6
- mobile 375px: ไม่ล้นจอ การ์ดยุบ 2 คอลัมน์ ปุ่มสลับไม่ล้น กราฟวาดครบ
- ไม่มี JavaScript error หรือ console.error

หมายเหตุที่ตรวจพบ: สัปดาห์ 14/9–20/9 วิธี V2 (เฉลี่ยของเฉลี่ยรายวัน) ให้ 151.2 เท่ากับวิธี V1 พอดี
เพราะจำนวนแถวแต่ละวันใกล้กัน จึงเขียนโน้ตในหน้าว่า "อาจไม่ตรงกัน" ไม่ใช่ "ไม่ตรงกันแน่นอน"

### เมนูไม่ถึงเป้า

ย้ายไปเป็นปุ่มแรกของกลุ่ม 3 คลาส `.nav-danger` (พื้นชมพู ขอบแดง แถบแดงซ้าย ตัวหนา) และมีป้ายจำนวนคนที่ไม่ถึงเป้า
ทดสอบ: วันล่าสุดขึ้น 20 · ขยายช่วง 01/08-17/09 ขึ้น 85 ตรงกับการ์ดในหน้า · ตอนเลือกเป็น gradient แดงของ V2

`node tests/calculation.test.cjs` ผ่าน · 15/09/2026 = 82,958 / 144.9 / 66 แถว ไม่ขยับ

## รอบก่อนหน้า: หน้าไม่ถึงเป้า + เรียงตาราง + แก้บั๊กจากการตรวจ (17/09/2569)

### หน้าไม่ถึงเป้า (แยกตามโซน)

อ่านต้นฉบับ V2 มาก่อนทำ พบว่า V2 มีหน้านี้จริง เป็นเมนูเต็มหน้า `data-page="belowtarget"` คลาส `nav-alert`
ตัวเรนเดอร์คือ `renderBelowTargetPage()` (app.js 5936-6033) กับ `drawBelowTargetChart()` (6035-6104)
V3 ลอกลำดับองค์ประกอบมาครบ การ์ด 5 ใบ → กราฟแท่งซ้อน → โน้ต → ตารางโซน → ตารางรายคน

ตรวจเลขด้วยการคิดซ้ำจากแถวดิบในเบราว์เซอร์ (เขียนกฎโซนและสูตรใหม่จาก ZONE_GROUPS + getZoneTarget เอง แล้วเทียบกับที่หน้าแสดง)

| ค่าที่ตรวจ | หน้าเว็บแสดง | คิดซ้ำจากแถวดิบ |
|---|---|---|
| คนที่นับได้ (17/09/2026) | 30 คน | 30 คน |
| ไม่ถึงเป้า | 20 คน | 20 คน |
| สัดส่วน | 66.7% | 66.7% |
| ช่องว่างเฉลี่ย | 61.5 หยิบ/ชม. | 61.5 |
| โซนที่มีคนไม่ถึงเป้า | 10 / 12 โซน | 10 / 12 |
| สามโซนแรก | CB-DB-DC-CC 4/5 (เป้า 200) · AH-AI 3/6 (เป้า 170) · AJ-AK 3/6 (เป้า 170) | ตรงกันทั้งสามรายการ |

- ขยายช่วงเป็น 01/08–17/09/2026: 85 ไม่ถึงเป้า จาก 105 คน (81.0%) · 14 โซน · กราฟวาดครบ · ไม่มี error ใน console
- กอง Not Found (คอลัมน์ AH ไม่ตรงกฎโซน V1) มี 31 คน ไม่ถึงเป้า 29 คน — ไม่ถูกเลือกเป็น "โซนที่ต้องดูก่อน"
  และมีข้อความเตือนในโน้ตว่าไม่ใช่โซนจริง ต้องเติม Zone ให้ถูกก่อนจึงเชื่อเลขกองนี้ได้
- Target ที่ใช้มาจาก `getZoneTarget()` จริง ตรวจแล้วว่า Half Rack ได้ 200 และ Full Rack/Pick to Sort ได้ 170 ตาม DEFAULT_TARGETS

### การเรียงตาราง

- หน้าพนักงานเก่า การ์ด Not Found: กด Productivity ครั้งแรกได้ 374.0 → 353.0 → 327.0 (มาก→น้อย)
  ครั้งที่สองได้ 29.4 → 46.3 → 47.0 (น้อย→มาก) ครั้งที่สามกลับเป็นลำดับตั้งต้น (เรียงตามยอดหยิบ 719,841 ลงมา)
- ค่าว่างไปท้ายรายการ: ตอนเรียงน้อย→มาก คนที่ไม่มีค่าเฉลี่ยไปอยู่หน้า 4 ไม่ได้ขึ้นก่อนแบบนับเป็น 0
- เรียงตามข้อความ (Zone) ได้ · กดเปิดฟอร์มหลังเรียงยังทำงาน

### บั๊กจากผลตรวจ 3 มุมมอง (8 ข้อ แก้ครบ)

- [สูง] `bind()` ผูก listener ทีละปุ่มครั้งเดียว ตารางวาด tbody ใหม่ทุกครั้งที่เปลี่ยนหน้า/ค้นหา ปุ่มชุดใหม่จึงกดไม่ติด
  ทดสอบหลังแก้: เปิดฟอร์มได้ทั้งหน้า 1 หน้า 3 หลังค้นหา และหลังเรียง ทั้งหน้าพนักงานเก่าและหน้า Not Found (209 รายการ 5 หน้า)
- [สูง] การ์ด "อัตราออกของคนใหม่" เอาค่าที่กรอกในเว็บไปปนแล้วอ้างว่ามาจากชีต Resigned (29.6% → 44.4%) แยกนับสองก้อนแล้ว
- [กลาง] `toIso` รับวันที่ที่ไม่มีจริง — ทดสอบหลังแก้: 31/02/2026, 30/02/2569, 31/04/2026, 29/02/2026, 9/9/9999, 1/1/28, 1/1/1899 ถูกปฏิเสธ
  ส่วน 31/08/2026, 31/08/2569, 2026-08-31, 31-08-2026, 29/02/2024, 1/1/26, 1/1/69 ผ่านถูกต้อง
- [กลาง] ยอด "กระทบ Total Pick" หดลง 91% เมื่อกดว่ามีคนออกแล้ว เพิ่มป้าย "ออกแล้วแต่ข้อมูลยังว่าง N คน · X ชิ้น"
- [กลาง] ปุ่มลบบอกว่าลบแล้วทั้งที่เขียน localStorage ไม่สำเร็จ
- [ต่ำ] ค่า date ที่ไม่ใช่สตริงในเครื่องทำการ์ดหายทั้งใบ · [ต่ำ] ป้ายนับซ้อนกันจนบวกไม่ตรง · [ต่ำ] ไม่ได้ขยับ `?v=` ของไฟล์ที่แก้

`node tests/calculation.test.cjs` ผ่านทุกรอบ · 15/09/2026 = 82,958 / 144.9 / 66 แถว ไม่ขยับ

## รอบก่อนหน้า: ที่กรอกสำหรับคนที่ลาออกไปแล้ว (17/09/2569)

คำถามคือ "ถ้าลาออกไปแล้วละจะให้กรอกตรงไหน" — ตรวจโค้ดแล้วคำตอบเดิมคือ **ไม่มีที่ให้กรอกเลย**
สถานะ "ออกแล้ว" อ่านจากชีต Resigned ทางเดียว เว็บเขียนกลับไม่ได้ และฟอร์มเติมข้อมูลมีแต่ช่องของชีต 2ND

วัดขนาดปัญหาจาก `data/snapshot.json` (อ่านเมื่อ 17/09/2569 01:59 · วันล่าสุด 16/09/2026)

| ตัวเลข | ค่า |
|---|---|
| รหัสที่มีผลงานใน Results Master | 288 รหัส |
| ไม่มีในทะเบียน 2ND | 208 รหัส |
| ในนั้นมีในชีต Resigned แล้ว | 121 รหัส |
| **ไม่มีทั้งใน 2ND และไม่มีทั้งใน Resigned** | **87 รหัส · Total Pick 3,252,753 ชิ้น (14.5% ของทั้งระบบ)** |
| ใน 87 รหัสนั้น เงียบเกิน 30 วัน | 72 คน (82.8%) — น่าจะออกไปแล้วจริง |
| ชีต Resigned ทั้งหมด | 176 แถว · 175 รหัส · 14 แถวไม่มีวันพ้นสภาพ |

ที่แก้

- `v2-roster-write.js` เพิ่มปุ่มสองตัวเลือก **ยังทำงานอยู่ / ⛔ ลาออกแล้ว** สลับชุดช่องกรอกและปลายทาง
  (2ND C–M ↔ Resigned A/B/H) พร้อมบอกปลายทางไว้ใต้ปุ่มตลอด
- คนที่กรอกว่าออกแล้วขึ้น Remark **สีส้ม "กรอกในเว็บ"** แยกจากสีแดงของชีต และถูกซ่อนตามปุ่มซ่อนคนที่ออกแล้ว
- การ์ด "รหัสที่ต้องตามข้อมูล" **ไม่ถูกปุ่มซ่อนคนที่ออกแล้วตัดรายการอีกต่อไป** (เดิมส่ง `shown` เข้าไป
  ทำให้งานค้างหายพร้อมคน และอาจขึ้นข้อความ "ข้อมูลครบทุกคน" ทั้งที่ยังไม่ครบ)
- คนที่ออกแล้วและรู้ชื่อแล้ว ไม่นับเป็นงานค้าง ทำให้รายการหน้าพนักงานเก่าลดจาก 231 เหลือ 117 คนที่ต้องตาม + 38 คนที่ออกแล้วแต่ยังไม่รู้ชื่อ
- คำเตือนตอนอ่านชีตไม่สำเร็จ (`source.warnings`) เดิมสร้างแล้วไม่มีใครแสดง ตอนนี้ขึ้นทั้งแถบสถานะแหล่งข้อมูลและในการ์ด

ตรวจในเบราว์เซอร์จริง (http://127.0.0.1:8093)

- รหัส 25159 หน้าพนักงานใหม่: กดเติมข้อมูล → ฟอร์มตั้งต้นเป็น "ยังทำงานอยู่" 10 ช่อง (2ND C–M)
- กด ⛔ ลาออกแล้ว → เหลือ 3 ช่อง (ชื่อ, วันที่พ้นสภาพ, หมายเหตุ) ป้ายปลายทางเปลี่ยนเป็น Resigned A/B/H
  ปุ่มบันทึกเปลี่ยนข้อความเป็น "บันทึกว่าออกแล้ว"
- กรอกวันที่ **31/08/2569** (พ.ศ.) → เก็บเป็น `2026-08-31` ถูกต้อง · กรอก "ไม่รู้" → เตือนและไม่บันทึก · เว้นว่างได้
- หลังบันทึก: ตารางรายชื่อขึ้น `⛔ ออกแล้ว 31/08/2026 · กรอกในเว็บ` + ชื่อขีดฆ่า
  แถบเกณฑ์ขึ้น "ออกแล้ว 9 คน (กรอกในเว็บ 1 คน)" · ป้ายการ์ดขึ้น "13 คนต้องตามข้อมูล … · กรอกในเว็บรอใส่ใน Sheet 1 คน"
- กด "ซ่อนคนที่ออกแล้ว": ตารางรายชื่อซ่อนคนนั้น แต่การ์ดรหัสที่ต้องตามข้อมูลยังครบ 21 แถวเท่าเดิม (ตรวจทั้งสองหน้า)
- รีโหลดหน้าแล้วที่กรอกไว้ยังอยู่ ฟอร์มจำสถานะและค่าเดิม มีปุ่มลบที่กรอกไว้
- หน้า Not Found & ตรวจข้อมูล: รหัส 10057342 ทำแบบเดียวกันได้ ขึ้น `⛔ ออกแล้ว ไม่ทราบวันที่ · กรอกในเว็บ` และนับแยกในแถบสรุป
- Export CSV: 16 คอลัมน์ มีคอลัมน์ "ปลายทางที่ต้องไปกรอก" บอก `ชีต Resigned (A รหัส, B ชื่อ, H วันพ้นสภาพ)`
  กับ `ชีต 2ND (C–M)` ตามสถานะ · วันพ้นสภาพออกเป็น `2026-08-31` · ชื่อรวมเป็นคอลัมน์เดียวกำกับ `2ND C / Resigned B`
- mobile 375×812: ปุ่มสองตัวเลือกไม่ล้นจอ (`scrollWidth = 375`) ช่องกรอกเรียงคอลัมน์เดียว
- `node tests/calculation.test.cjs` ผ่านทั้งชุด · 15/09/2026 = 82,958 / 144.9 / 66 แถว ไม่ขยับ
- ไม่มี JavaScript error ใน console · ล้างข้อมูลทดสอบใน localStorage ออกหลังตรวจเสร็จแล้ว

## รอบก่อนหน้า: ทำหน้าช่วงเวลาให้ใช้ข้อมูล H–AE เต็มที่ + ชื่อคนยึด Sheet 2ND (16/09/2569)

ผู้ใช้แจ้งว่าช่วงเวลามีข้อมูลอยู่แล้วใน Results Master และให้ใช้ Sheet 2ND สำหรับรายชื่อคน

ตรวจโครงคอลัมน์จริงจาก snapshot

- Results Master มี 43 คอลัมน์ A–AQ ช่วงเวลาอยู่ที่ H–AE (ดัชนี 7–30) รวม 24 ช่อง หัวตารางไล่ `7:00 - 8:00` ถึง `6:00 - 7:00`
- ยืนยันว่าข้อมูลครบ: ผลรวม 24 ช่อง **เท่ากับ Total Pick คอลัมน์ E พอดี** ทุกวันที่สุ่มตรวจ (15/09 = 82,958, 16/09 = 37,455, 15/05 = 85,775) และทุกแถวมีค่าในทั้ง 24 ช่อง
- ช่องที่เป็น 0 เป็นค่าจริงจากต้นทาง ไม่ใช่การอ่านพลาด เช่น 16/09 ช่วง 09:00–23:00 เป็น 0 เพราะเป็นข้อมูลระหว่างวัน ณ เวลาที่อ่าน
- 2ND (82 แถว, 81 รหัส): B รหัสพนักงาน, C ชื่อ-นามสกุล (ไทย), D ชื่อเล่น, E สังกัด, F หน้าที่, G วันที่เริ่มงาน, H Status Work, I วันที่จบ Training, J Zone, K Pick Type, L BU, M Ship กะ

คุณภาพชื่อใน Results Master (19,563 แถวที่มีวันที่)

| รายการ | จำนวน |
|---|---:|
| แถวที่ User ID ตรงกับ 2ND | 11,840 |
| แถวที่ User ID ไม่อยู่ใน 2ND (209 รหัส) | 7,723 |
| ชื่อในคอลัมน์ B สะกดต่างจาก 2ND | 357 |
| คอลัมน์ B เขียนว่า `Not Found Data` | 1,799 |
| คอลัมน์ B ว่าง | 2 |
| แถวที่ไม่อยู่ใน 2ND และคอลัมน์ B เขียนว่า `รายวัน` | 967 |

ตัวอย่างชื่อที่สะกดต่าง: `MPPTG0505` — Results Master เขียน “นางสาวฉัครวดี หนูแก้ว” ส่วน 2ND เขียน “นางสาวฉัตรวดี หนูแก้ว”

สิ่งที่แก้

- `metrics.js` เพิ่มชุด helper กลาง: `hourIndexes/hourLabels/hourValues/hourTotals` สำหรับช่วงเวลา และ `rosterMap/userId/personName/personNickname/inRoster/isPlaceholder` สำหรับทะเบียน
- ชื่อพนักงานยึด 2ND คอลัมน์ C เป็นหลัก ถ้า User ID ไม่อยู่ใน 2ND จึงใช้คอลัมน์ B ของ Results Master ตามกฎ V1 ที่ไม่ลบผลงานย้อนหลัง และถ้าคอลัมน์ B เป็นข้อความแทนค่าว่าง (`Not Found`, `Not Found Data`, `#N/A`, `-`, ว่าง) จะแสดงเป็น `Not Found` ป้ายเดียวให้สอดคล้องกับตัวกรองกะ
- หน้าช่วงเวลาเขียนใหม่: การ์ด KPI 4 ใบ (Total Pick คอลัมน์ E · ผลรวม 24 ช่องพร้อมบอกว่าตรงกับ E หรือไม่ · ชั่วโมงที่หยิบมากสุด · พนักงานที่มีงาน), กราฟ Chart.js สองแกนในการ์ด `.card.wide` (แท่ง = Total Pick ต่อช่วงเวลา, เส้น = จำนวนพนักงานที่มียอดในช่วงนั้น), ตารางรายชั่วโมง (Total Pick / สัดส่วน / จำนวนคน / คนที่หยิบมากสุดพร้อมยอด) และตารางรายคน 11 คอลัมน์ (User ID / ชื่อจาก 2ND + ชื่อเล่น / กะ / ชั่วโมงที่มีงาน x/24 / ช่วงแรก / ช่วงสุดท้าย / ช่วงที่หยิบมากสุด / Total Pick / ผลรวม 24 ช่อง / Productivity / Operating time) ค้นหาและ Export CSV ได้
- หน้า Not Found เพิ่มรายการตรวจ “ช่องชื่อใน Results Master ไม่ใช่ชื่อคน” เพื่อไล่ 1,799 แถวนั้นได้
- `v2-views.js` ใช้ helper ช่วงเวลาชุดเดียวกัน และชื่อในกราฟอันดับพนักงานยึด 2ND ด้วย
- ไม่แก้ `v1-engine.js` และไม่เปลี่ยนสูตรใด ๆ

ผลตรวจ

- หน้าช่วงเวลาวันที่ 15/09/2026: Total Pick (Column E) 82,958 · ผลรวม 24 ช่องเวลา 82,958 (ขึ้นข้อความ “ตรงกับ Column E พอดี”) · ชั่วโมงที่หยิบมากสุด `1:00 - 2:00` ที่ 6,465 ชิ้น · พนักงานที่มีงาน 75 คน · ตารางรายชั่วโมง 24 แถว · ตารางรายคน 75 รายการ
- ค้นหา `MPPTG0505` ในตารางรายคน ได้ “นางสาวฉัตรวดี หนูแก้ว · ชื่อเล่น ปลา” (ชื่อถูกจาก 2ND ไม่ใช่ตัวสะกดผิดใน Results Master) กะ A · มีงาน 3/24 ช่วง · 0:00-1:00 ถึง 2:00-3:00 · พีค 1:00-2:00 · Total Pick 187 · ผลรวม 24 ช่อง 187
- แถว 11:00–12:00 ที่ผู้หยิบไม่อยู่ใน 2ND แสดงป้าย `Not Found` แบบเดียวกับตัวกรองกะ (เดิมโชว์ข้อความ `Not Found Data` จาก Sheet)
- มือถือ 375×812: กราฟขึ้นครบ ป้ายแกนย่อเหลือ 8 จุด ไม่มีการล้นแนวนอน
- `node tests/calculation.test.cjs` ผ่าน · ไม่พบ JavaScript error ใน console

## รอบก่อนหน้า: รวมปุ่มกะ Not Found ให้เหลืออันเดียว (16/09/2569)

ผู้ใช้ถามว่าปุ่ม “Not Found” กับ “Not Found Data” มาจากไหน และควรมีแค่อันเดียว
ตรวจค่าดิบในคอลัมน์ AG (หัวตาราง `Shift`) ทั้ง 19,563 แถวที่มีวันที่ พบ 7 ค่า

| ค่าดิบใน AG | แถว | Total Pick | แถว AF > 0 | เฉลี่ย AF | ช่วงวันที่ที่พบ |
|---|---:|---:|---:|---:|---|
| `B` | 8,000 | 9,092,218 | 6,993 | 132.4 | 02/01/2026 – 16/09/2026 |
| `A` | 7,827 | 9,680,483 | 6,984 | 154.5 | 02/01/2026 – 16/09/2026 |
| `Not Found Data` | 1,632 | 1,935,877 | 1,283 | 127.2 | 02/01/2026 – 16/09/2026 |
| `รายวัน` | 969 | 794,467 | 875 | 110.2 | 02/01/2026 – 18/06/2026 |
| `C` | 899 | 797,714 | 562 | 182.2 | 02/01/2026 – 16/09/2026 |
| `Training` | 124 | 78,424 | 111 | 95.2 | 07/05/2026 – 07/06/2026 |
| เซลล์ว่าง | 112 | 102,836 | 109 | 121.3 | 08/06/2026 – 05/09/2026 |

ที่มาของปุ่มซ้ำ

- `Not Found Data` เป็นข้อความที่อยู่ในเซลล์ AG ของ Sheet จริง (สูตร/ผู้กรอกที่ต้นทางเขียนไว้)
- `Not Found` ไม่มีอยู่ใน Sheet เลย เป็นป้ายที่โค้ด V3 สร้างขึ้นแทนเซลล์ AG ที่ว่าง — มาจาก `insights.js` เดิมที่เขียน `String(r[32]||'').trim()||'Not Found'`

จึงกลายเป็นสองปุ่มที่ความหมายเดียวกันคือ “ไม่รู้กะ”

วิธีแก้

- เพิ่ม `V3Metrics.shiftKey(row)` ใน `metrics.js` ให้รวมค่าที่แปลว่าไม่รู้กะเป็นถังเดียวชื่อ `Not Found` ครอบคลุมเซลล์ว่าง, `Not Found`, `Not Found Data`, `#N/A` และ `-`
- `matches()` ใช้ `shiftKey()` เทียบตัวกรอง จึงมีผลทั้งฝั่ง Worker (KPI ของ `script.js`) และฝั่งหน้าเว็บ (`insights.js`, `v2-views.js`) ด้วยการแก้จุดเดียว เพราะ `data-worker.js` เรียก `V3Metrics.matches` ตัวเดียวกัน
- `insights.js` สร้างรายการกะจาก `shiftKey()` และ `v2-views.js` จับกลุ่มกราฟเทียบกะจาก `shiftKey()` เช่นกัน
- ค่าดิบยังแสดงตามต้นทางในตารางรายการ จึงตรวจย้อนได้ว่าแถวนั้นว่างหรือเป็นข้อความ `Not Found Data`
- ไม่แก้ `v1-engine.js` — สรุปกะของ V1 ยังจับกลุ่มด้วย `normalizeGroupLabel_()` ตามเดิม (เซลล์ว่าง = “ไม่ระบุกะ”, `Not Found Data` คงชื่อเดิม) ตามข้อตกลงว่าไม่แตะสูตร/การจับกลุ่มของ V1

ผลตรวจ

- ปุ่มกะเหลือ 7 ปุ่ม: ทุกกะ · 🅰️ กะ A · 🅱️ กะ B · กะ C · ⚠️ Not Found · กะ Training · กะ รายวัน
- ถัง `Not Found` รวม 1,744 แถว / Total Pick 2,038,713 = เซลล์ว่าง 112 แถว + `Not Found Data` 1,632 แถว (102,836 + 1,935,877)
- กดปุ่ม ⚠️ Not Found แล้วการ์ด KPI รายงาน “1,392 แถวเข้าเฉลี่ย · 352 แถวไม่เข้าเฉลี่ย · 1,744 แถวต้นทาง” ตรงกับผลรวมทั้งสองค่า
- กราฟเทียบกะเหลือแท่ง Not Found แท่งเดียวที่ 2,038,713
- `node tests/calculation.test.cjs` ผ่าน · ไม่มี JavaScript error ใน console

## รอบก่อนหน้า: แถบตัวกรองตรงตามต้นฉบับของ V2 (16/09/2569)

ผู้ใช้ส่งภาพแถบ `.sysbar` ของ V2 มาให้ทำตาม จึงจัดใหม่ให้เรียงและใช้คอนโทรลชนิดเดียวกัน
ระบบ → กะ → 🎯 เป้า Target → วันที่ → ช่วงสำเร็จรูป → ↻ รีเฟรช

สิ่งที่แก้

- `ระบบ` เหลือ 3 ปุ่มตามภาพ: ทั้งหมด (All) / Pick (PTT) / Pick to Sort (BPS) — ปุ่ม “⚠️ ไม่ระบุประเภท” ที่ V3 เคยมีถูกถอดออกจากแถบ (ค่ายังอยู่ใน `<select>` ที่ซ่อนไว้ และดูได้จากหน้า Not Found & ตรวจข้อมูล)
- `กะ` เปลี่ยนจาก `<select>` เป็นปุ่ม `.systog.shiftog` แบบ V2 สร้างจากค่าจริงในคอลัมน์ AG ของ Sheet — พบ 7 ค่า: A, B, C, Not Found, Not Found Data, Training, รายวัน ใช้อีโมจิ 🅰️/🅱️ เฉพาะ A/B เหมือน V2 และ ⚠️ นำหน้าค่ากลุ่ม Not Found โดยยังแสดงข้อความจริงเพื่อไม่ให้ปุ่มซ้ำกัน
- `เป้า Target` เปลี่ยนจากข้อความอ่านอย่างเดียวเป็นช่องกรอกเลข `#prodTargetInput` + “หยิบ/ชม.” + ปุ่ม ⚙️ ตั้งค่า Target ตามภาพ
- `วันที่` ใช้ `.datebar` สองช่องแบบ V2 และเปลี่ยนวันแล้วโหลดทันที (ปุ่ม “ใช้” ของ V3 ถูกซ่อนไว้ และถูกกดให้เองจาก `v2-shell.js`)
- ช่วงสำเร็จรูปเหลือ 3 ปุ่มตามภาพ: ทั้งหมด / Weekly / Monthly (เดิม V3 มี 5 ปุ่ม รวม ล่าสุด และ วันนี้) พฤติกรรมเปิดหน้าแล้วเลือกวันล่าสุดที่มีข้อมูลยังอยู่เหมือนเดิม แต่จะไม่มีปุ่มไฮไลต์จนกดเลือกช่วง
- เพิ่ม `[hidden]{display:none!important}` ใน `v2-theme.css` เพราะคลาส `.btn-apply` ของ V1 ทับค่า display ของแอตทริบิวต์ `hidden` ทำให้ปุ่มที่ซ่อนไว้ยังโผล่บนมือถือ
- บนจอ ≤900px จัดปุ่มกะเป็นตาราง 3 คอลัมน์ เพราะค่ากะของ Sheet มีมากกว่าของ V2 ถ้าเรียงแถวเดียวจะเบียดกัน

บั๊กที่เจอและแก้ไปพร้อมกัน

- `TARGETS` ใน `script.js` ประกาศด้วย `const` จึงไม่เป็นพรอปเพอร์ตีของ `window` ทำให้ `v2-views.js` และ `v2-shell.js` อ่านค่าไม่ได้และใช้ค่าสำรอง 170 ตายตัวมาตลอด แก้โดยผูก `window.TARGETS`, `window.DEFAULT_TARGETS`, `window.updateTargets` ไว้ใน `script.js` (อ้างวัตถุเดียวกัน ไม่ทำสำเนาค่า)
- ระหว่างแก้ไฟล์ `v2-shell.js` ถูกเขียนทับจนว่างเปล่าเพราะสคริปต์ช่วยงานพลาด จึงเขียนไฟล์ใหม่ทั้งไฟล์และตรวจ `node --check` ทุกไฟล์ JS ว่าผ่านและไม่มีไฟล์ใดขนาด 0 ไบต์

ผลตรวจ

- ตั้งช่อง Target เป็น 150: `TARGETS.overall` = 150, การ์ด KPI เป้าหมาย = 150, เส้น Target ในกราฟแนวโน้มเป็น 150, แกนขวาของกราฟเทียบกะปรับเป็น 210 และบันทึกลง localStorage แล้วตั้งกลับเป็น 170 เรียบร้อย
- กดปุ่ม 🅱️ กะ B: `#v3Shift` = B, ปุ่มติดสถานะ active, หัวเรื่องต่อท้าย “· กะ B”, ตัวกรองทำงาน แล้วกด “ทุกกะ” กลับได้ค่าเดิม Total Pick 37,455 / Productivity 167.7
- มือถือ 375×812: ปุ่ม “ใช้” ถูกซ่อนจริง (`display: none`), ปุ่มกะเป็นตาราง 3 คอลัมน์อ่านได้, ไม่มีการล้นแนวนอนทั้งหน้าและในแถบ
- ไม่พบ JavaScript error ใน console

## รอบก่อนหน้า: ถอด 7 เมนูออกชั่วคราว (16/09/2569)

ตามที่ผู้ใช้สั่งให้ตัด อันดับพนักงาน, ประเภทงาน & โซน, ทะเบียน & รายบุคคล, สังกัด, กะ & BU, รายการต้นทาง
และ สูตร & แหล่งข้อมูล ออก แล้วจะค่อย ๆ นำกลับ

วิธีที่ทำ

- ย้ายมาร์กอัปทั้ง 7 `<section>` ออกจาก `index.html` ไปเก็บที่ `parked/sections.html` พร้อมขั้นตอนนำกลับในหัวไฟล์
- จัดเมนูใหม่เหลือ 2 หมวดแบบ `.nav-group` ของ V2: หมวด 1 (ภาพรวมวันนี้, แนวโน้มรายเดือน, สรุปผล & Present) และหมวด 2 (แผนผังโซน, ช่วงเวลา, Training, เกิน 3 เดือน, Not Found & ตรวจข้อมูล)
- ก่อนถอด ไล่ทุกจุดที่ `script.js` / `insights.js` อ้าง element ของ 7 หน้านั้น พบว่ามีที่ไม่มี null guard จุดเดียวคือ `categoryGrid` ใน `renderCategoryCards()` จึงเพิ่ม `if (!categoryGrid) return;` และเพิ่ม guard ให้ `zonePage/recordsPage/staffPage/hoursPage/qualityPage` กับการผูก `onchange` ของตัวกรองใน `insights.js` เพื่อความปลอดภัยเวลาถอด/ใส่หน้ากลับ
- ไม่ลบตัวเรนเดอร์หรือสูตรใด ๆ — นำ `<section>` กับปุ่มเมนูกลับแล้วหน้าจะทำงานเองทันที

ผลตรวจหลังถอด

- นับใน `index.html`: เหลือ 8 `<section class="tab-panel">` และ 8 ปุ่มเมนู ตรงกัน
- ตรวจสมดุลแท็ก HTML ทั้งไฟล์: ไม่มีแท็กค้างและไม่มี mismatch
- คลิกครบทั้ง 8 เมนู ทุกหน้ามีความสูงจริงและชื่อบน `.pagehead` ถูกต้อง ไม่มี JavaScript error ใน console
- กราฟบนหน้าภาพรวมยังครบ 7 ใบ และค่า 15/09/2026 ยังเป็น Total Pick 82,958 / Productivity 144.9 / 66 แถวเข้าเฉลี่ย · 9 ไม่เข้าเฉลี่ย · 75 แถวต้นทาง
- `node tests/calculation.test.cjs` ผ่าน
- มือถือ 375×812: แถบเมนูแนวนอนแสดงครบ 8 รายการ ไม่มีการล้นแนวนอนของหน้า

## รอบก่อนหน้า: รื้อผังหน้าภาพรวมเป็นของ V2 จริง (16/09/2569)

รอบก่อนเปลี่ยนแค่เปลือก (Sidebar/หัวเรื่อง/แถบกรอง/สี) เนื้อในหน้าภาพรวมยังเป็นองค์ประกอบของ V1
ตรวจแล้วพบว่า `#tab-overview` ของ V3 มีลำดับ element ตรงกับ V1 ทุกบรรทัด จึงรื้อใหม่

สิ่งที่แก้รอบนี้ (ชั้นแสดงผลเท่านั้น ไม่แตะ `v1-engine.js` และไม่แก้ตรรกะการคำนวณใน `script.js`)

- `index.html`: หน้าภาพรวมเปลี่ยนเป็นผัง overview ของ V2 — แถบแหล่งข้อมูลสีเขียว → `.kpis` การ์ด `.kpi` 5 ใบ → `.card.wide` กราฟแนวโน้ม (มี `.seg` + แถบเลื่อนเดือน) → `.card.wide` รายชั่วโมง → แถวโดนัทระบบ + แผงสถิติ และแท่งประเภทงาน → แถว BU + สังกัด → การ์ดเทียบกะ; เพิ่มการ์ดกราฟอันดับพนักงานแบบ V2 ในหน้าอันดับพนักงาน; เพิ่ม Chart.js 4.4.1 + datalabels 2.2.0 จาก cdnjs เวอร์ชันเดียวกับ V2
- ถอดบล็อกของ V1 ออกจากหน้าภาพรวม: เกจวงกลม, mini-chart 2 ใบ, แผงรายวันทั้งเดือนแบบ SVG, snapshot grid
- `v2-views.js` (ใหม่): วาดกราฟทั้งหมดด้วย Chart.js โดยรวมยอดผ่าน `V3Metrics` ซึ่งเป็นสูตร V1 (`aggregate()` = ผลรวม E, ผลรวม AF ของแถว AF>0 ÷ จำนวนแถวนั้น) ไม่เขียนสูตรใหม่
- `v2-theme.css`: เพิ่มพื้นหลังปุ่ม `.systog` ที่ไม่มี `data-sys` ให้เห็นสถานะ active

ตรวจความปลอดภัยก่อนถอด element: ไล่ทุก `querySelector/getElementById` ใน `script.js`/`insights.js` แล้วยืนยันว่าที่ไม่มี null guard คือ
`#overallCard`, `#overallStatus`, `#overallGap`, `#overallProgress`, `#activeDateLabel`, `#activeDateHint`
(บวกชุด `.sysbar` และคอนเทนเนอร์ของ insights) — ทั้งหมดยังอยู่ในหน้า ส่วนที่ถอดออกล้วนมี guard
ยืนยันผลจริงหลังแก้: ไม่มี JavaScript error ใน console

## เทียบกับ Google Sheets โดยตรง

อ่าน Results Master ช่วง C19424:AF19498 (75 แถวของวันที่ 15/09/2026) จาก connector แบบ UNFORMATTED_VALUE:

| รายการ | Google Sheets | V3 |
|---|---:|---:|
| Total Pick (Column E) | 82,958 | 82,958 |
| จำนวนแถว AF > 0 | 66 | 66 |
| เฉลี่ย AF | 144.9090909090909 | 144.9 (แสดง 1 ตำแหน่ง) |

9 แถวที่ไม่เข้าเฉลี่ยยังอยู่ใน Total Pick ตามสูตร V1 ไม่ใช่การยกเว้นสินค้าของ V2

## Automated tests

`node tests/calculation.test.cjs` ผ่านหลังเปลี่ยนหน้าตา: สูตรเฉลี่ยแบบ sum/count, ยอดรวมและรายวัน, Not Count, วันเริ่ม BPS, วันปฏิทิน, กะ C, รหัสตัวเลขผสมตัวอักษร, CSV quoted fields, ทะเบียนเต็ม และตัวกรองระบบ

ผลจาก snapshot:

| วันที่ | Total Pick | Productivity | แถวเข้าเฉลี่ย |
|---|---:|---:|---:|
| 02/01/2026 | 48,453 | 102.7 | 59 |
| 15/05/2026 | 85,775 | 120.3 | 82 |
| 15/09/2026 | 82,958 | 144.9 | 66 |
| 16/09/2026 | 37,455 | 167.7 | 27 |

วันที่ 16/09 เป็นข้อมูลระหว่างวัน ไม่ใช่ยอดปิดวัน ตัวเลขสดอาจเพิ่มหลังเวลาที่อ่าน

## Browser checks (เบราว์เซอร์จริงบน http://127.0.0.1:8093)

ตัวเลขบนผังใหม่ — ตั้งช่วงวันที่ 15/09/2026 แล้วอ่านค่าจาก DOM และจาก object ของ Chart.js

| จุดที่อ่าน | ค่า |
|---|---|
| KPI ยอดหยิบรวม (Total Pick) | 82,958 |
| KPI Productivity หลัก (เฉลี่ย AF) | 144.9 |
| KPI พนักงานที่มีผลงาน | 75 · หมายเหตุ “66 แถวเข้าเฉลี่ย · 9 แถวไม่เข้าเฉลี่ย · 75 แถวต้นทาง” |
| แท่งกราฟแนวโน้มของวันที่ 15/09 | 82,958 (สีเข้ม = อยู่ในตัวกรอง) |
| จุดเส้น Productivity ของวันที่ 15/09 | 144.9 |
| ป้ายรวมโดนัทระบบ | รวม 82,958 ชิ้น (PTT 72,298 + BPS 3,682 + ไม่ระบุประเภท 6,978) |

ตรงกับการตรวจต้นทาง 82,958 / 144.9 / 66 แถวทุกจุด และ `node tests/calculation.test.cjs` ยังผ่านหลังแก้

- เปิดครั้งแรกโหมด “ล่าสุด” = 16/09/2026 แสดง 37,455 และ 167.7 ตรงกับตาราง snapshot
- โหมดรายวันกางทั้งเดือน ก.ย. 2569 ครบ 30 วัน เส้น Productivity อ่านได้ 15/09 = 144.9, 16/09 = 167.7 และป้ายบอก “ทั้งเดือน 1,096,037 · ในตัวกรอง 82,958 Total Pick”
- ปุ่ม `Pick to Sort (BPS)` บนแถบ `.systog`: ค่าเปลี่ยนเป็นชุด BPS ขึ้นข้อความ “BPS เริ่มนับ 08/06/2026 ตาม V1” และหัวเรื่องต่อท้าย “· Pick to Sort (BPS)”
- หน้าอันดับพนักงาน: ปุ่ม Top 12/20/50 เปลี่ยนจำนวนแท่งได้จริง (12 → 20) หมายเหตุใต้กราฟรายงาน “20 คนแรกจาก 66 User ID · ถึง Target 170 แล้ว 13 คน”
- ตรวจกราฟทั้ง 7 ใบบนหน้าภาพรวมด้วย `Chart.getChart(...)`: trend, hourlyPeakChart, cat, storageTypeChart, buShareChart, macroAffiliationChart, shiftCompareChart ขึ้นครบทั้ง desktop และ mobile

หน้าตา

- ตรวจครบทุกเมนู 15 หน้า ทุกหน้ามีความสูงจริงและเรนเดอร์การ์ด/ตารางครบ
- สแกนพื้นหลังทุกองค์ประกอบที่แสดงอยู่: ไม่พบพื้นหลังโทนมืดของธีมเดิมเหลืออยู่ มีเพียงแถบวัดผลและ tile โซนที่เป็นสีตามค่าโดยตั้งใจ
- Sidebar ติดขอบซ้ายแบบ sticky (วัด `getBoundingClientRect().top = 0` ขณะเลื่อนหน้า)
- Target modal, ตัวกรอง Training/เกิน 3 เดือน, ตารางรายการ 50 แถวต่อหน้า และปุ่ม Export CSV ยังทำงาน
- desktop 1600×1000 และ mobile 375×812: บนมือถือใช้กฎของ V2 คือ Sidebar เป็นแถบเลื่อนแนวนอน ตัวกรองเรียงเต็มความกว้าง KPI 2 คอลัมน์ วัดแล้วไม่มีการล้นแนวนอนของหน้า (`documentElement.scrollWidth = clientWidth = 375`) กราฟรายวัน 30 แท่งย่อป้ายแกนเหลือ 8 จุด ไม่ทับกัน ตารางกว้างเลื่อนอยู่ในกรอบของตัวเอง
- ไม่พบ JavaScript error ใน console ตลอดการทดสอบ

## ขอบเขต

เป็นการตรวจสูตรและตัวอย่างข้อมูล ไม่ใช่การรับรองความถูกต้องของทุกค่าที่ผู้ดูแลกรอกใน Sheet หาก AF หรือทะเบียนผิด V3 จะแสดงตามต้นทางพร้อมหน้าตรวจข้อมูล ไม่สร้างค่าทดแทนเอง

V3 อ่านข้อมูลอย่างเดียว ไม่แก้ Sheet ไม่แก้โครงการ V1/V2 (อ่านไฟล์ของ V2 เพื่อคัด CSS เท่านั้น) และไม่ลบข้อมูลหรือทรัพยากร BigQuery ยังไม่ได้เผยแพร่หรือ push ไป Git
